[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "etl_package"
version = "0.1.0"
description = "Music pipeline ETL package for Databricks Asset Bundle"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
